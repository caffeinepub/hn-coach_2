// Re-export from the library package for backward compatibility
export {
  useInternetIdentity,
  InternetIdentityProvider,
} from "@caffeineai/core-infrastructure";
export type {
  InternetIdentityContext,
  Status,
} from "@caffeineai/core-infrastructure";
